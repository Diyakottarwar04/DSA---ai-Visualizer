import React from "react";

const ChatAssistant = () => (
  <div style={{ padding: "2rem" }}>
    <h2>AI Assistant</h2>
    <p>This page will be connected to GPT via FastAPI.</p>
  </div>
);

export default ChatAssistant;
